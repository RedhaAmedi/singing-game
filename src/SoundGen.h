#ifndef SOUNDGEN_H_
#define SOUNDGEN_H

// Function to start generating a continuous wave sound with a specified frequency.
// Returns: An integer value, possibly a modified frequency
int startSound(int freq);

// Function to stop the continuous wave sound generation
void stopSound();

#endif  
